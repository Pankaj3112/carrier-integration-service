# Carrier Integration Service

A shipping carrier integration service built in TypeScript that wraps carrier APIs (starting with UPS) to fetch normalized shipping rates.

## Setup
```bash
npm install
cp .env.example .env
```

## Scripts
```bash
npm test
npm run build
```